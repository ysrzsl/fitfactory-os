[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt && python3 src/seed_data.py"

[deploy]
startCommand = "python3 -m uvicorn src.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
